#15. Create Three Vectors (Numeric, Character, Logical) and Display Types
num_vec <- c(1.1, 2.2, 3.3)
char_vec <- c("A", "B", "C")
log_vec <- c(TRUE, FALSE, TRUE)

print(num_vec)
print(class(num_vec))

print(char_vec)
print(class(char_vec))

print(log_vec)
print(class(log_vec))
