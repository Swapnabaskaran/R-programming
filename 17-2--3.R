#17/3(3)
# Define four columns of data
data <- c(1:24)

# Create an array with 3 rows, 4 columns, and 2 tables
my_array <- array(data, dim = c(3, 4, 2))

# Display the content of the array
print("Created 3D Array:")
print(my_array)
